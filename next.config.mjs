/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['assets.leetcode.com'], // Allow images from LeetCode
    },
  };
  
  export default nextConfig;
  